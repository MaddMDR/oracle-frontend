# Dependencies — never commit these (huge, auto-installed by npm install)
node_modules/

# Next.js build output — regenerated by next build
.next/
out/

# Environment files — contains secrets, different per machine
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel/

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo
next-env.d.ts
